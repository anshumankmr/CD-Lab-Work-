int sum(int x, int y) {
	int a[10];
	int b[10];

	printf("User defined function\n");
	scanf("%d %d", &x, &y);
	for(i=10; i<a[i]; i--) {
		for(j=10; j<b[i]; j--) {
			return (x+y);
		}
	}
}

int main() {
	int a;
	int b;
	int num[10];
	int num2[20];
	if (a <= b) {
		a = num[10];
	} 
	else
		a = num2[0];
	while( num1[0] != 0)
		printf("num=%d", a);
}